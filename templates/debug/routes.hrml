<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <title>📌 Route registrate</title>
    <style>
        body { font-family: sans-serif; padding: 2rem; }
        table { border-collapse: collapse; width: 100%; }
        th, td { border: 1px solid #ccc; padding: 0.5rem; text-align: left; }
        th { background-color: #f0f0f0; }
    </style>
</head>
<body>
    <h1>📌 Route registrate</h1>
    <table>
        <thead>
            <tr>
                <th>Endpoint</th>
                <th>URL</th>
                <th>Metodi</th>
            </tr>
        </thead>
        <tbody>
            {% for route in routes %}
            <tr>
                <td>{{ route.endpoint }}</td>
                <td>{{ route.rule }}</td>
                <td>{{ route.methods }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>